{
  "name": "web-dieta",
  "version": "1.0.0",
  "private": true,
  "description": "Generador de planes de dieta y ejercicio para invitados",
  "scripts": {
    "install:all": "npm install --prefix backend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "sync:pages": "powershell -ExecutionPolicy Bypass -File scripts/sync-pages-root.ps1"
  }
}
